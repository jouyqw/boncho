# 본초죽염 칼럼 발행 플레이북 — bonchojw.com/column

매일 자동 세션(클라우드)이 그대로 따라 실행하는 지침입니다.
목표: **검색(구글·네이버)과 AI 검색(GEO)에서 오래 살아남는 고품질 건강 정보 칼럼**을 매일 발행.

## ★ 절대 원칙 (컴플라이언스 — 위반 시 발행 금지)

소금·죽염은 **식품**입니다. 식품에 질병 치료·예방이나 체중 감량 효과를 표방하면 식품표시광고법 위반이며, 검색엔진도 저품질·스팸으로 봅니다. 그래서:

1. **의학적 효능 단정 금지**: "염증이 낫는다 / 살이 빠진다 / 사마귀가 없어진다 / 면역력을 높인다 / 혈압을 낮춘다 / 부작용 없다 / 100% / 무조건" 등 금지. `validate-content.mjs` 가 자동 차단합니다.
2. **가짜 후기·체험 위장 금지**: "내돈내산"처럼 브랜드 글을 소비자 후기로 위장하지 않습니다. 브랜드 정보 칼럼으로 떳떳하게 씁니다.
3. **정보 중심**: 효능이 아니라 "올바른 사용법·사실 비교·생활 정보·죽염과 일반 소금의 차이"를 다룹니다. 검색량 높은 키워드는 제목·질문형 소제목으로 노리되 본문은 과장 없이.
4. **링크는 투명하게**: 카페·제품 링크는 렌더러가 하단에 "커뮤니티 후기·문의 / 제품 보기"로 자동 삽입합니다. 본문에 몰래 심지 않습니다.

## 매일 실행 순서

1. 저장소 루트에서 시작. `TZ=Asia/Seoul date +%F` 로 오늘 날짜 확인.
2. `content/columns.json` 의 기존 slug·카테고리를 확인.
3. `content/topic-bank.json` 에서 아직 없는 slug 를 고름. 소진되면 원칙에 맞는 새 주제를 직접 생성(slug 중복 금지).
   - **발행 페이스**: 하루 3편이며, 한 세션이 3편을 몰아 쓰지 않습니다. 예약 세션 세 개가
     1시간 간격(KST 07:37 / 08:37 / 09:37)으로 각각 1편씩 씁니다. 세션마다 `git pull` 후
     기존 slug 를 확인하고, 최근 발행 카테고리를 피해 순환하세요.
4. 각 글을 `content/columns.json` 배열 끝에 추가(아래 형식).
5. 생성: `node scripts/generate-columns.mjs`
6. 검증: `node scripts/validate-content.mjs` — **반드시 통과**. 실패하면 원인을 고쳐 다시.
7. 커밋 & 푸시:
   ```
   git add -A
   git -c commit.gpgsign=false commit -m "칼럼 발행: <오늘> 2편"
   git push origin main
   ```
   push 하면 Cloudflare 가 자동 배포합니다. (git push 가 프록시 인증으로 막히면, 프록시를 거치지 않는 직접 연결로 재시도하고, 그래도 안 되면 실패를 그대로 보고하세요.)

## columns.json 항목 형식

```json
{
  "slug": "english-lowercase-hyphen",
  "title": "질문형/검색의도 반영 제목(중복 금지)",
  "description": "45~160자 요약. 키워드 자연 포함.",
  "category": "구강·생활 관리 | 죽염 이야기 | 소금·나트륨 상식 | 요리·활용 | 보관·고르는 법",
  "keywords": ["키워드1","키워드2","키워드3"],
  "datePublished": "YYYY-MM-DD",
  "dateModified": "YYYY-MM-DD",
  "lead": "결론부터 말씀드리면, ...(요지 답변 1~2문장)",
  "body": [ ...블록 배열... ],
  "faqs": [{"q":"...","a":"..."}, ...(3개 이상)],
  "related": [{"href":"/column/<기존 slug>/","label":"..."}]
}
```

### body 블록
- `"문자열"` → 문단(`<p>`). 강조는 `**텍스트**`.
- `{"type":"heading","text":"질문형 소제목"}`
- `{"type":"summary","title":"핵심 요약","items":["...","..."]}`
- `{"type":"table","headers":[...],"rows":[[...]]}`
- `{"type":"list","items":[...]}`
- `{"type":"infographic","title":"...","caption":"...","items":[{"icon":"<svg .../>","title":"...","text":"..."}]}` (아이콘은 `viewBox='0 0 24 24' width='20' height='20' fill='none' stroke='currentColor' stroke-width='1.8'` 인라인 SVG, 홑따옴표)
- `{"type":"callout","label":"팁","text":"..."}` / `{"type":"warning","label":"주의","text":"..."}`
- `{"type":"help","title":"이 글이 도움이 되셨다면","text":"..."}`

## 품질 기준 (SEO + GEO)
- **분량**: 본문 텍스트 2,000자 이상(검증기 최소 1,500자).
- **구조**: heading(질문형) 4~6개, summary 1개, 비주얼(인포그래픽·표·콜아웃·주의) 2종 이상 권장.
- **GEO(AI 인용 대비)**: 소제목은 이용자가 실제로 검색·질문하는 문장형으로, 각 소제목 첫 문단은 요지부터 1~2문장으로 답한 뒤 풀어씀.
- **E-E-A-T**: 도입부는 실제 상담·문의에서 자주 나오는 구체적 장면으로 시작. 근거 있는 생활 정보와 올바른 사용법 중심.
- **독창성**: 기존 글과 다른 각도. 문단 복붙 금지. 유사도 85% 미만(검증기 자동).
- **내부링크**: related 로 실제 존재하는 다른 칼럼 1~2개 연결(토픽 클러스터).

## 발행 전 자가 점검(모두 '예'여야 push)
- 효능 단정·질병 치료·체중감량·면역력 표현이 없는가
- 가짜 후기 톤이 아닌가(브랜드 정보 칼럼인가)
- 소제목이 질문형이고 첫 문단이 답부터인가
- 본문 2,000자 이상 / 비주얼·FAQ 갖췄는가
- `generate-columns.mjs` → `validate-content.mjs` 통과했는가

## 검색엔진 등록(최초 1회, 사람이)
- 구글: Search Console 에 `bonchojw.com` 등록 후 `sitemap.xml` 제출.
- 네이버: 서치어드바이저에 사이트 등록 후 `sitemap.xml` 제출.
- 이후 매일 새 글은 sitemap 을 통해 자동 수집됩니다.
